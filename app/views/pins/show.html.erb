<p id="notice"><%= notice %></p>

<p>
  <strong>Opis:</strong>
  <%= @pin.description %>
</p>
<%= image_tag @pin.image.url(:medium) %><br/>
<% if @pin.user == current_user %>
<%= link_to 'Edytuj', edit_pin_path(@pin), class: 'btn btn-link ' %> |
<% end %>
<%= link_to 'Cofnij', pins_path, class: 'btn btn-link ' %>
